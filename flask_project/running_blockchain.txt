These are the instructions which should have the sample block chain up and running 

1. install python 3.6 
2. install pipenv: pip install pipenv 
3. Create a virtual which has a version of python3.6: pipenv --python=python3.6
4. Install requirements: pipenv install 
5. Run the Server: pipenv run python blockchain_app.py
